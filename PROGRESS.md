[x] - No AssetsCatalog.tsx , para a tab de Workflows , criei um arquivo 'database.ts' para não trazer dados mockados no código , leve todo o objeto de Migração Vanilla para um arquivo , **não perca nenhum dado que já esteja na tela** migre todos os dados do workflow para o database.ts

[x] - Com o arquivo de database.ts populado , faça todo a tipagem para conseguirmos usar o Migração Vanilla 

[x] - Agora liste o workflows do database.ts 

[x] - Crie um Context com nome de 'WorkflowsProvider' , a responsabilidade desse context vai controlar todos os steps de um Workflow , então deveremos ter metodos para adicionar um workflow , listar todos os workflows , avançar etapas no workflow e avançar status

[x] - Na home.tsx agora a listagem do card de próximos ele devem ser listados atráves do WorkflowsProviders.tsx , trazer o último workflow selecionado 

[x] - No arquivo de WorkflowTrackerList , remova todos os mock do código

[x] - No WorkflowTrackerList.tsx , deve ser listado através do WorkflowProvider.tsx 

[x] - No database.ts , no onboardingSteps o adicione uma nova prop 'completedOnClick' — 2026-05-15T00:00:00Z · adicionado `completedOnClick: boolean` ao tipo OnboardingStep.

[x] - Os items que terão completedOnClick true , serão `step-01-install-cli` e `step-02-permission-cloud` — 2026-05-15T00:00:00Z · só esses dois passos têm completedOnClick=true; demais são false.

[x] - No Card de 'Próximo Passos' , adicione para cada item um botão clicável que será um CTA para iniciar a ação especifica . Também esse texto do botão irá vir do database.ts , então adicione uma nova prop no item com o título do botão e já popule ele com CTA  que faça sentido para cada item — 2026-05-15T00:00:00Z · prop `ctaLabel` adicionada e populada em todos os 8 passos; botão CTA renderizado em cada linha não concluída.

[x] - No Card  de 'Próximo Passos' na home.tsx , faça o cada item clicavél . Porém esse item será marcado como completed no click somente se ele estiver marcado como completedOnClick — 2026-05-15T00:00:00Z · linha + botão chamam `handleStepActivate`, que só dispara `advanceStep` quando o passo em curso tem completedOnClick=true.

[x] - Ajuste para no WorkflowTrackerList.tsx o item ser exibido , após o Step de `step-04-configure-workflow` ser marcado como true , ele também deve ser completado ao clicar no botão . Com isso , gere no database.ts um prop linked para dentro do `migrationWorkflow` para ser esse workflow que irá ser trigado no WorkflowTrackerList.tsx , crie dados mockados para esse Workflow porém que correspondem com a realidade — 2026-05-15T00:00:00Z · `migrationExecutionWorkflow` (4 verbos da Operação Vanilla) adicionado e ligado via `migrationWorkflow.linked`; step-04 vira completedOnClick=true; ao concluir step-04, Home dispara `addWorkflow(template.linked)` e a instância aparece no Workflow Tracker; cada linha do tracker tem botão "Avançar/Concluir" que chama `advanceStep`.

[x] - Ajuste para o step de `step-07-promote-hml` fazer o lançamento de um worflow que ira ser listado no WorkflowTrackerList.tsx . — 2026-05-15T00:00:00Z · trigger lift para `OnboardingStep.triggers`; `hmlPromotionWorkflow` (canário 5/50/100 + smoke) ligado em step-07; step-07 vira completedOnClick=true e dispara a execução ao concluir.

[x] - Limpe todo o `ApplicationHub.tsx` remova todos os mocks datas — 2026-05-15T00:00:00Z · array inline `apps[]` removido; tela passa a importar `applicationHubs` e tipos `ApplicationHub` / `ApplicationHubHealth` de `data/database.ts`.

[x]- Agora faça um novo item dentro do database.ts que será um `ApplicationHubNotification` , esse item deverá tem um icon + title + description + CTA + link + ApplicationHubId + ProjectID. Esse link vai levar para a listagem do ApplicationHub , ou seja , precisa estar linkado com um ApplicationHub existente . — 2026-05-15T00:00:00Z · tipo `ApplicationHubNotification` (icon como string lucide-name pra não acoplar à UI) + FK validável `applicationHubId` apontando pra `ApplicationHub.id`.

[x] - Com o `ApplicationHubNotification` criado , popule item com um notifição que o ApplicationHub de homologação foi provisionado . Com um CTA para ver a saúde da Aplicação em Homolog — 2026-05-15T00:00:00Z · `notif-hml-provisioned-ssa-pix-core` com icon CheckCircle2, CTA "Ver saúde em HML" e link `/application-hub/ssa-pix-core?env=hml`.

[x] - On finish the workflow at `WorkflowsProvider` , create item at `applicaitonHubs` on `database.ts` . Populate with mock datas , but make more realist infos — 2026-05-15T00:00:00Z · `WorkflowsProvider` agora possui `applicationHubs` state (seedado pelo array em `database.ts`); `advanceStep` detecta o frame de transição pra `completed` e gera um `ApplicationHub` plausível a partir dos inputs do template (sa_id → projectId/squad via lookup, métricas de app recém-promovido). Dedupe por `hub.id`. `04-ApplicationHub.tsx` agora consome `useWorkflows().applicationHubs` em vez do import estático.

[x] - No fluxo de `step-03-select-repos` , no Buscar Repo , substitua por "Busca por seu Serviço de Aplicação" , esse serviço de aplicação irá trazer vários , pois a ideia da arquitetura desse mono-repo é que cada parte da aplicação vai estar em repo , por exemplo : um repo para o código , outro para definição de ci/cd , outro para definição de infra e etc. — 2026-05-15T00:00:00Z · Step 1 do modal vira "Busca por Serviço de Aplicação"; mock SA `ssa-pix-core` retorna 5 repos da família (código, ci/cd, infra, db, config) com stack + size + último commit por repo.

[x] - No fluxo de `step-03-select-repos` , no Estrutura Mono-repo , traga a visualização no DIFF antes como era a visualização do multi-repo e como vai ficar no mono-repo , preciso que mantenha a estrurua de visualização em folder e sub-folder. — 2026-05-15T00:00:00Z · Step 2 mostra Antes (5 árvores de repo empilhadas) vs Depois (mono-repo unificado em apps/ssa-pix-core/{src,ci,infra,db,config}); TreeNodeView mantido (expand/collapse, ícones Folder/FolderOpen).

[x] - No fluxo de `step-03-select-repos` adicionar no modal um terceiro step que a ideia dele é trazer um lista de débitos técnico que serão solucionado ao fazer a migração para mono repo , foque em débito técnico plaúsivel de infra . — 2026-05-15T00:00:00Z · Step 3 lista 6 débitos infra (Jenkins free-form, secrets em repo, sem auto-rollback, imagens sem CVE scan, dashboards ad-hoc, schema manual) com pareamento "Hoje" / "Pós-migração"; header/footer suportam navegação 1→2→3.

[x] - No fluxo de `step-04-configure-workflow` dentro do objeto de `migrationExecutionWorkflow` adicione um novo item para que seria um stepper agêntico que para continuar o fluxo o usuário vai precisar aprovar . — 2026-05-15T00:00:00Z · novo step `agentic-java-21-upgrade` inserido entre verb-build e verb-deploy; `OnboardingStep.agentic` carrega PR metadata (título, autor, summary, files com hunks); verb-deploy.dependsOn ajustado.

[x] - Com essa mudança o Stepper agêntico no `WorkflowTrackerDetails` irá precisar de aprovação , faça esse stepper do React Flow ter um 'Accepted' Ou 'Declined' para esse fluxo agêntico mostre um atualização simple : Aceitar um pr para atualizar de Java 8 para Java 21 , esse ao clicar no Stepper que seja exibido nos detalhes os diff dessa PR , seja o mais realista possível . — 2026-05-15T00:00:00Z · `Verb` agora inclui `agentic` (Sparkles + cor accent); nó renderiza Aprovar/Declinar inline quando approval=pending; clicar abre BottomSheet com `AgenticPrDiff` mostrando lista de arquivos + hunks +/− linha-a-linha (pom.xml java.version, Dockerfile imagem base, switch-expression em PaymentService, record PaymentDto). Edges roteiam build → agentic → deploy.

[x] - Dentro do WorflowsProvider.tsx , agora trazer uma nova prop no Workflow que está no 'Próximo Passos' . Essa prop é um array `pendingAgenticFlow` a ideia aqui popular esse item dentro — 2026-05-15T00:00:00Z · `PendingAgenticFlowItem` (stepId, stepTitle, prTitle, prAuthor) adicionado ao `WorkflowInstance`; `buildInstance` varre `template.onboardingSteps` filtrando por `agentic` e seeda o array.

[x] - Agora sempre que tiver Workflow que necessita de uma aprovação agêntica , faça esse fluxo agentico ser exibido dentro do Home.tsx no Card . — 2026-05-15T00:00:00Z · Home agora deriva `pendingAgenticItems = instances.flatMap(wf.pendingAgenticFlow)`, marcado com `workflowInstanceId` + `workflowName`; renderiza no card "Fluxos agênticos pendentes aprovação" (cada linha é um `Link` pra `/workflows/<id>` com ícone Sparkles, badge "agêntico", PR title e autor); empty state mantido.

[x] - No fluxo `step-04-configure-workflow` , faça que esse fluxo agentico , seja notificado dentro do Card da Home.tsx . Deixe esse fluxo de onboarding mais real possível . — 2026-05-15T00:00:00Z · `PendingAgenticFlowItem` enriquecido com prSummary, filesChanged, linesAdded/Removed (computados dos hunks) e createdAt; row da Home agora mostra timing relativo ("agora"/"há Ns"), pulsing live dot quando fresh (<60s), summary multi-linha, contagem de arquivos e diff +N/−N. Disparado via `triggers: migrationExecutionWorkflow` em step-04 — `buildInstance` popula o array no momento da criação da instância da execução.

[x] - Ao finalizar o fluxo de `Migração Vanilla` 100% , remova toda a referencia dele no Card de 'Proóximo passos' e adicione um `Alert` de sucess , avisando que o Fluxo foi concluído deixe uma mensagem de parabéns , enfim deixei esse `Alert` bem congrats — 2026-05-15T00:00:00Z · novo componente `CongratsAlert` (gradiente success→accent, confete SVG, ícone PartyPopper) substitui o OnboardingCard quando `lastWorkflow?.status === 'completed'`; mostra "Parabéns, Luigi! 🎉", SA name, contadores success (passos, pipeline, canário) e CTAs pra Application Hub + Workflow Tracker.

[x] - Após finalizar o fluxo de `Migração Vanilla` , adicione uma aplicação no `Aplication Hub` . Com o nome da aplicação dele . — 2026-05-15T00:00:00Z · `ApplicationHub.name` adicionado ao tipo + populado por `toFriendlyAppName(sa)` no `buildHubFromTemplate` (ssa-pix-core → "Pix Core"); tabela em `04-ApplicationHub.tsx` mostra o nome amigável com SA em mono abaixo, coluna renomeada de "SA" para "Aplicação".

[x] - Após aprovação do fluxo agêntico , remova a aprovação do fluxo agêntico — 2026-05-15T00:00:00Z · novo método `resolveAgenticItem(workflowId, stepId)` no `WorkflowsProvider` filtra o item do `pendingAgenticFlow`; `WorkflowTrackerDetail.handleApprove`/`handleDecline` chamam com `wfId` (URL param) + `AGENTIC_TEMPLATE_STEP_ID` (derivado do template), removendo a linha do card "Fluxos agênticos pendentes aprovação" na Home após decisão humana.

[x] - Após finalizar o fluxo de `Migração Vanilla` adicione no database.ts um item no array de `applicationHubs` para mostrar a aplicação do usuário na tabela do `ApplicationHub.tsx` , mas somente após fluxo de `Migração Vanilla` está 100% finalizado — 2026-05-15T00:00:00Z · `advanceStep` no frame de finish agora checa `!executionTemplateIds.has(template.id)` antes de criar hub + alerta; só template primário (`migrationWorkflow`) dispara — execuções ligadas (`migrationExecutionWorkflow`, `hmlPromotionWorkflow`) terminam em silêncio. Hub só aparece quando Migração Vanilla atinge 100%.

[x] - Adicione esse step como uma nova prop de final-step `step-07-promote-hml` , ele vai ser o steo quando o usuário já tiver finalizado todos os step , ele vai ficar como sendo um último CTA , mas o fluxo já está 100% completo . — 2026-05-15T00:00:00Z · prop `finalStep?: boolean` adicionada ao `OnboardingStep` em database.ts; step-07-promote-hml virou `finalStep: true`; `OnboardingCard` separa `regularSteps` vs `finalSteps`, calcula progresso só sobre os regulares (passos não-finais), e renderiza o finalStep em uma seção dedicada abaixo da lista com badge "último passo" + label "fluxo 100% completo" quando todos os regulares estão concluídos.

[x] - Faça com que os items de `ApplicationHub.tsx` sejam visíveis quando o usuário estiver no `final-step` — 2026-05-15T00:00:00Z · `WorkflowsProvider.advanceStep` agora detecta entrada em step com `finalStep: true` (além de detectar conclusão) e dispara `buildHubFromTemplate` + `setApplicationHubs` antecipadamente; guard `!executionTemplateIds.has` continua válido, então só `migrationWorkflow` (primário) dispara. Alerta de "infra parcial" continua emitindo só na conclusão (post-mortem). Efeito: tabela do `ApplicationHub.tsx` mostra a aplicação assim que o usuário avança pra step-07, antes mesmo de clicar "Promover pra HML".

[x] - Ao entrar no `final-step` da Migração Vanilla, marcar a trajetória do usuário como concluída — 2026-05-15T00:00:00Z · `advanceStep` agora trata `enteringFinal` como `reachedEnd`: o finalStep é eager-completed (status `done` + completedAt) e o workflow vira `status: 'completed'` no mesmo ciclo. Hub + alerta de Application Hub criados pelo mesmo branch unificado (`trajectoryEnded`). Trigger do finalStep (`hmlPromotionWorkflow`) também dispara automaticamente via `buildInstance` no provider (o usuário não clica mais no CTA do finalStep), com dedupe por `templateId`. Resultado: ao concluir step-06, Home já mostra `CongratsAlert`, ApplicationHub exibe `Pix Core`, e o Workflow Tracker lista a promoção HML.